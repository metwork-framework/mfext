name: test

on:
  push: {}

jobs:
  test:
    runs-on: self-hosted
    steps:
      - name: checkout
        uses: actions/checkout@v2
      - name: bootstrap
        run: |
          docker run -v .:/src centos:6 /bin/bash -c "cd /src ; ls ; mkdir -p /opt/metwork-mfext-master ; ./bootstrap.sh /opt/metwork-mfext-master"
      - name: bootstrap2
        run: |
          docker run -v .:/src centos:6 /bin/bash -c "cd /src ; cat adm/root.mk"
